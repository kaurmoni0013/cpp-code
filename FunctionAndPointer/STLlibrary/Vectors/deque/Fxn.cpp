#include <iostream>
#include <deque>
using namespace std;
int main(){
    deque<int> deq;
    deque<int> deq={1,2,3,4,5};
    

}


// size, erase, clear, begin, end, rbegin,
// rend, insert, front, back all are same as vector
// list=>doubly linked list(random access not posible)
// deque=>double ended queue (insertion and deletion from both ends) dynamic array=>random access posible